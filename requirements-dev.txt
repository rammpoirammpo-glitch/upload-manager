# Development / CI-only dependencies. NOT installed in the production image.
httpx>=0.27        # FastAPI TestClient
ruff>=0.4          # linter
moto[s3]>=5.0      # in-memory AWS S3 for multipart/resume tests
